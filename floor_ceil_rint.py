# First Attempt

import numpy as np

np.set_printoptions(legacy="1.13")

A = list(map(float, input().split()))
a = np.array(A)
print(np.floor(A), np.ceil(A), np.rint(A), sep="\n")

# Solution

import numpy as np

np.set_printoptions(legacy="1.13")

A = list(map(float, input().split()))
a = np.array(A)
print(np.floor(A), np.ceil(A), np.rint(A), sep="\n")

# Got it right!
